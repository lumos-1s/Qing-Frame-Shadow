<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qingframe.server.mapper.PasswordResetMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO password_reset (user_id, email, code, used, expires_at)
        VALUES (#{userId}, #{email}, #{code}, #{used}, #{expiresAt})
    </insert>

    <select id="findLatestByEmail" resultType="com.qingframe.server.entity.PasswordReset">
        SELECT id, user_id, email, code, used, expires_at, created_at
        FROM password_reset
        WHERE email = #{email}
        ORDER BY id DESC
        LIMIT 1
    </select>

    <update id="markUsed">
        UPDATE password_reset
        SET used = 1
        WHERE id = #{id}
    </update>

</mapper>
